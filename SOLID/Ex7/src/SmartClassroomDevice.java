/**
 * Marker interface retained for backward compatibility only.
 * All capabilities are now expressed through ISP-compliant interfaces:
 * Powerable, Dimmable, TemperatureControllable, AttendanceScannable, InputConnectable.
 */
public interface SmartClassroomDevice {
    // intentionally empty — see capability interfaces
}
